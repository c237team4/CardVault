<%- include('partials/header', { title: 'Login - CardVault' }) %>

<!-- ============================================================
     LOGIN  |  Owner: Tan Boon Meng (25052694)

     WHAT THE ROUTE GIVES YOU  (app.js, GET /login):
        messages   array of SUCCESS strings from req.flash('success')
                   e.g. 'Registration successful! Please log in.'
        errors     array of ERROR strings from req.flash('error')
                   e.g. 'Invalid email or password.'

     Two separate variables on purpose -- one is good news, one is bad
     news, and they should not look the same.

     WHAT IT MUST DO:
       1. Form posts to  /login  with method POST
       2. Two inputs, name= must match exactly:
             name="email"      email
             name="password"   password
       3. If messages has anything, show it as a SUCCESS (green)
       4. If errors has anything, show it as an ERROR (red)
       5. A link to /register for people with no account yet

     TEST IT:
        node app.js  ->  http://localhost:3000/login
        - register first             -> green 'Registration successful'
        - wrong password             -> red 'Invalid email or password'
        - ryan@demo.sg / demo123     -> lands on /dashboard
        - admin@cardvault.sg / admin123 -> lands on /admin-dashboard
     ============================================================ -->

<%# TODO: write the form here %>


<%- include('partials/footer') %>
